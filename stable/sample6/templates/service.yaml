apiVersion: v1
kind: Service
metadata:
  name: {{ include "sample6.fullname" . }}
  labels:
    {{- include "sample6.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sample6.selectorLabels" . | nindent 4 }}

apiVersion: v1
kind: Service
metadata:
  name: {{ include "sample7.fullname" . }}
  labels:
    {{- include "sample7.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sample7.selectorLabels" . | nindent 4 }}

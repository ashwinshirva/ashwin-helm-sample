apiVersion: v1
kind: Service
metadata:
  name: {{ include "sample5.fullname" . }}
  labels:
    {{- include "sample5.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sample5.selectorLabels" . | nindent 4 }}
